name: "[Workflow] PR Environment"

defaults:
  run:
    shell: bash

on:
  push:
    branches:
      - 'main'

permissions:
  contents: write


jobs: 
  foo:
    runs-on: ubuntu-latest
    steps:
    - name: semver tag
      id: semver_tag
      uses: ministryofjustice/opg-github-actions/.github/actions/semver-tag@1f8fc17d658d17f3bd8f3e066e5a936d131af2cf
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        with_v: true
  
    - name: "echo stuff"
      if: always()
      run: |
        ls -la /home/runner/work/export-php-fpm-metrics-cloudwatch/export-php-fpm-metrics-cloudwatch/_ghbranchnameaction/
        pwd
        sudo apt install -y tree
        tree -a ${{github.workspace}}
        ls -la ${{github.workspace}}
